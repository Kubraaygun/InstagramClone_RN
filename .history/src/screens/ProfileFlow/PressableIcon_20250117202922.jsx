import {StyleSheet, TouchableOpacity} from 'react-native';
import React from 'react';

const PressableIcon = () => {
  return (
    <TouchableOpacity>
      <Text>PressableIcon</Text>
    </TouchableOpacity>
  );
};

export default PressableIcon;

const styles = StyleSheet.create({});
